# Bobby Yankou
# Artificial Intelligence
# Project #1
# Checkers
# Check Move

def check_move(dest, piece, board):
    import re
    
    #first see if dest is directly diagonal to piece
    #if not check if is two diagonal off from piece then if opponent between
    #also check if piece is moving in correct direction (if not king)
    #finally check that no more jumps available for piece, else prompt player

    

    return dest;
